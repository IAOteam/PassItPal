services:
  # Backend Service Configuration
  backend:
    build:
      context: ./server
    container_name: passitpal-backend-container
    ports:
      - "5001:5001"
    environment:
      # --- All Backend Variables Now Listed ---
      - MONGO_URI=${MONGO_URI}
      - REDIS_URL=redis://redis:6379 
      - PORT=5001
      - CLIENT_URL=http://localhost:5173

      - JWT_SECRET=${JWT_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}

      - GOOGLE_MAPS_API_KEY=${GOOGLE_MAPS_API_KEY}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GOOGLE_OAUTH_REDIRECT_URI=http://localhost:5001/api/auth/google/callback

      - TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID}
      - TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN}
      - TWILIO_PHONE_NUMBER=${TWILIO_PHONE_NUMBER}

      - CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
      - CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
      - CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}

      - RAZORPAY_KEY_ID=${RAZORPAY_KEY_ID}
      - RAZORPAY_KEY_SECRET=${RAZORPAY_KEY_SECRET}
      
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_SECURE=${SMTP_SECURE}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - EMAIL_FROM=${EMAIL_FROM}
    volumes:
      - ./server/src:/app/src
    depends_on:
      - mongo
      - redis # Make backend wait for Redis to be ready

  # Frontend Service Configuration
  frontend:
    build:
      context: ./frontend
    container_name: passitpal-frontend-container
    ports:
      - "5173:80"
    environment:
      - VITE_BACKEND_URL=http://backend:5001/api
      - VITE_Maps_API_KEY
      - VITE_GA_MEASUREMENT_ID

    depends_on:
      - backend

  # Development Database Service Configuration
  mongo:
    image: mongo:latest
    container_name: mongo-db-container
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db
  redis:
    image: redis:7-alpine # Use the official Redis image
    container_name: redis-cache-container
    ports:
      - "6379:6379" # Expose Redis port to your local machine if needed
    volumes:
      - redis-data:/data

volumes:
  mongodb-data:
  redis-data: #  Define a volume for Redis data persistence