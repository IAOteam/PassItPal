// This file runs before each test and is a great place for global setup.

// Extends Jest's `expect` with powerful matchers for asserting on the DOM
// e.g., expect(element).toBeInTheDocument();
import '@testing-library/jest-dom';
