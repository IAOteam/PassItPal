{
  "name": "passitpal-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "frontend",
    "packages/types"
  ],
  "scripts": {
    "build": "pnpm turbo run build",
    "test": "pnpm turbo run test",
    "lint": "pnpm turbo run lint",
    "dev": "pnpm turbo run dev"
  },
  "devDependencies": {
    "shx": "^0.3.4",
    "turbo": "^2.0.7",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@8.15.5"
}
