{
  "name": "passitpal-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "frontend",
    "packages/types"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}