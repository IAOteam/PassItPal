{
  "name": "passitpal-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "frontend",
    "packages/types"
  ],
  "scripts": {
    "build": "npm run build -w @passitpal/types && npm run build -w frontend"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}